@model TypeOfMaterialCreateViewModel

@{
    ViewData["Title"] = "Vytvořit typ materiálu";
}

<h2 class="text-center text-danger my-4">Vytvoření typu materiálu</h2>

<div class="row justify-content-center">
    <div class="col-md-6">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()

            <div class="mb-3">
                <label asp-for="Type" class="form-label fw-bold"></label>
                <input asp-for="Type" class="form-control form-control-sm w-75" />
                <span asp-validation-for="Type" class="text-danger"></span>
            </div>

            <div class="d-flex justify-content-end gap-2 mt-4">
                <a asp-action="Index" class="btn btn-sm btn-outline-danger px-4 py-2">Zpět</a>
                <button type="submit" class="btn btn-sm btn-danger px-4 py-2">Vytvořit</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
